Create the Step-05 HAPA-based digital intervention plan from this evidence bundle:

EVIDENCE_BUNDLE_JSON:
$EVIDENCE_BUNDLE_JSON

Important:
- The final intervention must explicitly connect selected targets, barrier domains, and coping strategies.
- Use the `hapa_component_candidates.components[*]` entries to structure choices per HAPA component.
- Prioritize component/subtree candidates with the strongest evidence and avoid dumping all available ontology items.
- The plan must remain implementable in a digital health setting.
- Include one user-facing personalized intervention message that is standalone and ready to display in-app.
- The personalized intervention message must contain 4-6 complete sentences and include a concrete next action, a barrier-trigger coping rule, and an end-of-day reflection step.
- Ensure the phased HAPA plan is detailed, practical, and coherent with the data.
- If critic_feedback is present, revise weak areas explicitly while preserving valid parts of the prior proposal.
